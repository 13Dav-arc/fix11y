<div class="card {{#if isFeatured}}featured{{/if}}">
  <img src="{{user.avatarUrl}}">
  {{#if user.hasEmail}}
    <label>Email</label>
    <input type="email" name="user-email" value="{{user.email}}">
  {{/if}}
  <div onclick="handleDelete('{{user.id}}')" class="btn-delete"></div>
</div>
